defaults:
  - task: lift
  - model: rssm_small
  - diffusion: diffusion_demo
  - _self_

exp_name: eval_diff_lift
seed: 0

# env
episodes: 20
max_steps: 500

# eval
eval:
  mode: rhc

# checkpoints
wm_checkpoint: runs/wm_lift/checkpoints/best.pt
diff_checkpoint: runs/diff_lift/checkpoints/diffusion_epoch_9.pt

# paths
run_dir: runs

hydra:
  run:
    dir: .
  output_subdir: null
